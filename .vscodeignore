# Ignore everything by default
**/*

# --- Explicitly INCLUDE ---
# Core extension code bundle (generated by esbuild)
!out/**
# Webview UI build output (generated by vite)
!webview-ui/dist/**
# Static resources (icons, etc.)
!resources/**
# Essential package metadata files
!package.json
!README.md
!CHANGELOG.md
!LICENSE

# --- Explicitly EXCLUDE (even if covered by **/* above, for clarity/safety) ---
# Source code (bundled into out/ or webview-ui/dist/)
src/**
webview-ui/src/**
webview-ui/public/**

# Node modules (dependencies are bundled)
node_modules/**
webview-ui/node_modules/**

# Build, config, and tooling files
**/tsconfig.json
**/tsconfig.node.json
**/eslint.config.mjs
webview-ui/postcss.config.js
webview-ui/tailwind.config.js
webview-ui/vite.config.*
.eslintrc.json
.eslintignore
.prettierrc
.prettierignore
.editorconfig

# VS Code specific development/test files
.vscode/**
.vscode-test/**
**/.vscode-test.*

# Test files/folders (not needed in packaged extension)
**/test/**
**/tests/**
**/__tests__/**
**/__mocks__/**
**/coverage/**

# Git / OS / Editor specific files
.git/**
.github/**
.husky/**
**/.DS_Store
**/.idea/**

# Other development/project files
.gitignore
.yarnrc
.npmrc
vsc-extension-quickstart.md

# Log files
**/*.log

# Packaged extension files (avoid nesting/including previous builds)
**/*.vsix
dist/**

# Intermediate build files (if any)
out/**/*.tsbuildinfo
